'use strict';

let carsIndex = $(".cars-index");
const initDataSetSize = 20;
const initDataSet = createDataSet(initDataSetSize);

// console.log(dataSet);

const carsIndexItems = initDataSet.map( (car) => {

});
